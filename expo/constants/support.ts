/**
 * Contact details for the dock2door support / admin team.
 * Used by the shared SupportMenu (call & email fall back to these when a
 * thread-specific contact isn't available).
 */
export const SUPPORT_PHONE = '+15551234567';
export const SUPPORT_EMAIL = 'support@dock2door.com';
