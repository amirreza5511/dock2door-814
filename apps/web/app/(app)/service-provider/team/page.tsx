"use client";

import { TeamView } from "@/components/team-view";

export default function ServiceProviderTeamPage() {
  return (
    <TeamView
      title="Team"
      subtitle="Invite crew and manage who can act on service jobs."
      companyType="ServiceProvider"
    />
  );
}
